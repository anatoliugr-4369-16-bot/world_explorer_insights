import 'package:flutter/material.dart';

class ExplorerBoardScreen extends StatelessWidget {
  const ExplorerBoardScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Explorer Board')),
      body: const Center(child: Text('Explorer Board (coming soon)')),
    );
  }
}
